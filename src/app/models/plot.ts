export interface Plot {
  id: string | number;   // unikalne ID działki
  name: string;          // nazwa działki (np. "Działka 12/7 – Gdynia")
  client: string;        // klient przypisany do działki
}
